import React from "react";
import styles from './footer.module.scss';

const Footer:React.FC = ()=>{
    return(
        <div className={styles.footer}>this footer</div>
    )
}
export default Footer;